import { AcquireAction } from "../../core/model";
export default class ForceReDrawByUserId extends AcquireAction {
    constructor(namespaceName: string, showcaseName: string, userId?: string | null);
}
