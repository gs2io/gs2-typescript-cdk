export default class StoreSubscriptionContentModelRef {
    private readonly namespaceName;
    private readonly contentName;
    constructor(namespaceName: string, contentName: string);
    grn(): string;
}
